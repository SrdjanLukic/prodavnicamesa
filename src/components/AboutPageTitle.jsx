import React from "react";

const AboutPageTitle = ({ text }) => {
    return (
        <h1 className="my-8 text-center text-3xl text-headingColor font-bold">
            {text}
        </h1>
    );
};

export default AboutPageTitle;
